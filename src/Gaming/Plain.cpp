#include "Plain.h"

namespace MgBall::Gaming
{
    Plain::Plain()
    {
    }

    void Plain::Tick()
    {
        ActorBase::Tick();
    }

    void Plain::Draw(const DrawingContext& context)
    {
        ActorBase::Draw(context);
    }
}
